import React from "react";
import CardForm from "../../components/CardForm";

const AddCardPage = () => {
  return (
    <div>
      <CardForm mode="add" />
    </div>
  );
};

export default AddCardPage;
